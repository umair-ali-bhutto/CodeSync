<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

	<Properties>
		<!-- Centralize log file path -->
		<Property name="LOG_DIR">/mnt/8EFED7B1FED79037/UBUNTU-BACKUP/Desktop/ag-mw-logs/</Property>
		<Property name="LOG_FILE">CodeSync</Property>
	</Properties>

	<Appenders>
		<!-- Console Logging -->
		<Console name="Console" target="SYSTEM_OUT">
			<PatternLayout
				pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1} - %msg%n" />
		</Console>

		<!-- Rolling File Logging -->
		<RollingFile name="FileLogger"
			fileName="${LOG_DIR}/${LOG_FILE}.log"
			filePattern="${LOG_DIR}/${LOG_FILE}-%d{yyyy-MM-dd}-%i.log"
			ignoreExceptions="false">

			<PatternLayout>
				<Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5p %c{1} [%t] %m%n</Pattern>
			</PatternLayout>

			<Policies>
				<!-- Roll file every day -->
				<TimeBasedTriggeringPolicy interval="1"
					modulate="true" />
				<!-- Also roll if file exceeds 100 MB -->
				<SizeBasedTriggeringPolicy size="100 MB" />
			</Policies>

			<DefaultRolloverStrategy max="100" />
		</RollingFile>
	</Appenders>

	<Loggers>
		<!-- Your application logs -->
		<Logger name="com.ag" level="debug" additivity="false">
			<AppenderRef ref="FileLogger" />
			<AppenderRef ref="Console" />
		</Logger>

		<!-- Spring logs (only errors) -->
		<Logger name="org.springframework.boot" level="error"
			additivity="false">
			<AppenderRef ref="Console" />
		</Logger>

		<!-- Root logger -->
		<Root level="error">
			<AppenderRef ref="FileLogger" />
			<AppenderRef ref="Console" />
		</Root>
	</Loggers>
</Configuration>
